# DataLens Backend — pinned production dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Settings & validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Auth — verify Supabase access tokens (HS256 JWT) for the Cockpit endpoints
PyJWT>=2.8.0

# PostgreSQL
psycopg2-binary>=2.9.0

# LLM — new google-genai SDK (replaces deprecated google-generativeai)
# Do NOT add google-api-core here; google-genai no longer uses it and
# importing google.api_core.exceptions will raise ModuleNotFoundError on
# a clean Vercel install.
google-genai>=1.0.0

# Timezone database — guarantees zoneinfo("Asia/Jerusalem") on minimal Vercel
# images (used to format Calendly booking times in Israel time for WhatsApp).
tzdata>=2024.1

# Testing (CI only — not needed in production image)
pytest>=8.0.0
httpx>=0.27.0          # required by FastAPI TestClient
pytest-mock>=3.14.0
